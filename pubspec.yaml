name: payflow_hive
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #Icons
  cupertino_icons:
  font_awesome_flutter:

  #Google
  google_fonts: 
  firebase_core:
  google_ml_kit:
  google_sign_in:

  #Mobx
  mobx: 
  flutter_mobx:

  #Hive
  hive:
  hive_flutter:

  #Notification
  awesome_notifications: ^0.0.6+10

  #Others
  shared_preferences:
  image_picker: 
  camera: 
  flutter_masked_text2: 
  animated_card: 
  path_provider: 
  flutter_launcher_icons: ^0.9.2

dev_dependencies:
  hive_generator: 
  build_runner: 
  mobx_codegen: 
  flutter_test:
    sdk: flutter

flutter_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/images/logomini.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/logomini.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
